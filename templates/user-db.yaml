apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: user-db
  labels:
    app: user-db
spec:
  serviceName: "user-db-svc"
  replicas: 1
  selector:
    matchLabels:
      app: user-db
  template:
    metadata:
      labels:
        app: user-db
    spec:
      containers:
        - name: mysql
          image: mysql:8.0
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: root
            - name: MYSQL_DATABASE
              value: userdb
          ports:
            - containerPort: 3306
              name: mysql
          volumeMounts:
            - name: user-db-data
              mountPath: /var/lib/mysql
  volumeClaimTemplates:
    - metadata:
        name: user-db-data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 500Mi
---
apiVersion: v1
kind: Service
metadata:
  name: user-db-svc
  labels:
    app: user-db
spec:
  ports:
    - port: 3306
      targetPort: 3306
  selector:
    app: user-db
  clusterIP: None
